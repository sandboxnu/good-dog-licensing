interface SceneInfoPageProps {
  params: {
    projectId: string;
    sceneId: string;
  };
}

export default async function SceneInfoPage(/*{ params }: SceneInfoPageProps*/) {
  //const { projectId, sceneId } = params;

  return <div>PLACEHOLDER</div>;
}
